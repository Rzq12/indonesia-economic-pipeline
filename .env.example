# For Docker Compose (local) — individual vars
DB_USER=pipeline
DB_PASSWORD=changeme

# For managed Postgres (Supabase/Neon) — set DATABASE_URL, takes priority
# DATABASE_URL=postgresql://user:password@host:5432/dbname